# Remote mode intentionally inherits compose.yaml unchanged. The base topology
# maps ai-router directly to REMOTE_OLLAMA_HOST, where the production router
# owns Responses, tools, reasoning, vision, discovery, and active-model
# translation. Keep this overlay as an explicit mode marker in Compose labels
# for backward-compatible deployment-mode inference.
x-dsh-deployment-mode: remote-direct
