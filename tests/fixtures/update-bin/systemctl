#!/bin/sh
# Test stub: fixtures never have a reachable user systemd bus, so every
# invocation fails. This keeps install-boot-service.sh on its documented
# bus-unreachable path no matter where the suite runs.
echo "systemctl: test stub refuses to reach a user bus" >&2
exit 1
